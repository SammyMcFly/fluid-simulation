app-title = Sci-PHi
about = About
repository = Repository
view = View
simulation = Simulation
plotting = Plotting
git-description = Git commit {$hash} on {$date}
ts_info = Time Step Info
logs = Logs
close = Close

# Dialogs
dialog =
    .cancel = Cancel
    .stop-close = Stop & close
    .stop-continue = Stop & continue
    .what-recording-and-rendering = recording and rendering
    .what-recording = recording
    .what-rendering = rendering
    .title-stop = Stop {$what}?
    .body-close = Closing the window will stop the current {$what}. Continue?
    .body-reload = Reloading will stop the current {$what}. Continue?
    .body-visualization = Changing the visualization will stop the current {$what}. Continue?

# Native file dialogs
file-dialog =
    .save-screenshot = Save Screenshot
    .save-state = Save State
    .filter-png = PNG Image
    .filter-ron = RON File

# Top bar tooltips
tooltip =
    .reload-simulation = Reload Simulation
    .reset-camera = Reset Camera
    .screenshot = Screenshot
    .save-state = Save Simulation State
    .save-plot = Save Plot
    .step-back = Step Back
    .step-forward = Step Forward
    .play = Play
    .pause = Pause
    .inspector = Inspector
    .settings = Settings

# Settings section titles
section =
    .fluid = Fluid
    .boundary = Boundary
    .cut-planes = Cut Planes
    .playback = Playback

# Settings item labels
settings =
    .fluid = Fluid
    .quantity = Quantity
    .colormap = Colormap
    .min-label = Min: 0
    .max-label = Max:
    .min-x = Min X
    .min-y = Min Y
    .min-z = Min Z
    .max-x = Max X
    .max-y = Max Y
    .max-z = Max Z
    .dx = dx
    .apply-bounds = Apply Bounds & Grid Edge Length
    .apply = Apply
    .sensor-plane-info = Cut planes define the sensor planes.
    .boundary = Boundary
    .hide-boundary = Hide boundary
    .boundary-alpha = Boundary alpha
    .cut-boundary = Cut boundary
    .discard-past-auto = Discard past automatically
    .play-reversed = Play reversed
    .loop = Loop
    .discard-buffered-past = Discard buffered past
    .discard-now = Discard now

# Fluid visualization options
fluid-vis =
    .mesh-uniform = Triangle mesh (uniform)
    .mesh-fluid-id = Triangle mesh (fluid ID)
    .samples-uniform = Samples (uniform)
    .samples-fluid-id = Samples (fluid ID)
    .samples-quantity = Samples (quantity)
    .sensor-plane = Sensor plane

# Scalar quantities
quantity =
    .speed = Speed
    .volume = Volume
    .density = Density
    .density-error = Density error
    .pressure = Pressure
    .kinetic-energy = Kinetic energy

# Boundary visualization options
boundary-vis =
    .mesh-original = Triangle mesh (original)
    .mesh-uniform = Triangle mesh (uniform)
    .mesh-boundary-id = Triangle mesh (boundary ID)
    .samples-uniform = Samples (uniform)
    .samples-boundary-id = Samples (boundary ID)
